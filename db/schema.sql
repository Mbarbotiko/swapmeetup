DROP DATABASE IF EXISTS swaps_db;
CREATE DATABASE swaps_db;

-- Run code above to create database in mySQL.  Then run server so MySQL creates the models (will create tables as well)  open the seeds file and run all the commands on it.